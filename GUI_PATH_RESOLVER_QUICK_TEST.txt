# GUI Path Resolver - Quick Test Guide

## Quick Test Commands

### 1. Test Path Resolver Integration
```bash
cd C:\Users\I763940\icbv-fragment-reconstruction
python test_gui_path_resolver.py
```

Expected output: All tests pass with [PASS] status

### 2. Launch GUI
```bash
python launch_gui.py
```

### 3. Test GUI Buttons

#### Test: Load Sample Data
1. Click "Load Sample Data" button
2. Should load 5 sample fragments
3. Should show success message

#### Test: Browse Folder
1. Click "Browse Folder" button
2. Should open dialog in data directory
3. Select data/sample folder
4. Should load fragments

#### Test: Load Config
1. Go to Parameters tab
2. Click "Load Config" button
3. Should open dialog in config directory
4. Should show .json files

#### Test: Save Config
1. Go to Parameters tab
2. Click "Save Config" button
3. Should open dialog in config directory
4. Save with a test name

#### Test: Run Assembly
1. Load sample data first
2. Click "Run Assembly" button
3. Should create output files in output/ directory
4. Should create log files in logs/ directory

#### Test: View Results
1. After running assembly
2. Go to Results tab
3. Click "Load Latest Results" button
4. Should display assembly images

## File Locations to Verify

### Development Mode
```
C:\Users\I763940\icbv-fragment-reconstruction\
├── config/           ← Config dialogs open here
├── data/sample/      ← Sample data loads from here
├── output/           ← Results saved here
└── logs/             ← Logs saved here
```

### What Changed

1. **Config dialogs** - Now use `get_config_file('')`
2. **Sample data** - Now use `get_sample_data_dir()`
3. **Browse folder** - Now use `get_data_dir()`
4. **Output files** - Now use `get_output_dir()`
5. **Log files** - Now use `get_log_dir()`
6. **Results loading** - Now use `get_output_dir()`

## Success Criteria

- [x] All path resolver tests pass
- [x] GUI launches without errors
- [x] Load Sample Data button works
- [x] Browse Folder button works
- [x] Load/Save Config buttons work
- [x] Run Assembly creates files in correct locations
- [x] Results tab displays images

## Troubleshooting

### Import Error
If you see import errors:
```bash
# Ensure path_resolver.py is in src/
ls -la src/path_resolver.py
```

### Sample Data Not Found
```bash
# Verify sample data exists
ls -la data/sample/*.png
```

### Output Directory Issues
```bash
# Check output directories exist
ls -la output/
ls -la logs/
```

## Key Files Modified

1. `src/gui_components.py` - Main GUI components file
   - Lines 24-29: Added path_resolver imports
   - Lines 391-398: Config load dialog
   - Lines 412-420: Config save dialog
   - Lines 734-741: Browse folder logic
   - Lines 830-868: Sample data loading
   - Lines 1082-1089: Output/log directories
   - Lines 1862-1894: Results loading

2. Created test file:
   - `test_gui_path_resolver.py` - Comprehensive test suite

3. Created documentation:
   - `GUI_PATH_RESOLVER_INTEGRATION.md` - Full documentation
   - `GUI_PATH_RESOLVER_QUICK_TEST.txt` - This quick reference

## Quick Verification

Run this one-liner to verify everything:
```bash
python test_gui_path_resolver.py && echo "All tests passed! GUI is ready."
```

If tests pass, the GUI is ready for both development and frozen modes!
